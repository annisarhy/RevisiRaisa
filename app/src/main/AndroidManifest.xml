<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.revisiraisa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SaveData"></activity>
        <activity android:name=".ReviewKuis" />
        <activity android:name=".DataKuis"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".RowQuestion" />
        <activity android:name=".ReviewPengajar" />
        <activity android:name=".RowPengajar" />
        <activity
            android:name=".DataPengajar"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>